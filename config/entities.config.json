[
  {
    "key": "client",
    "label": "Client",
    "endpoint": "/clientsList",
    "cron": "*/15 * * * *",
    "mode": "incremental",
    "enabled": true,
    "externalIdField": "client",
    "sourceUpdatedAtField": "latestSaveDate",
    "readStrategy": "date_window",
    "rangeFromParam": "latestSaveFrom",
    "rangeToParam": "latestSaveTo",
    "rangeWindowUnit": "day",
    "rangeWindowSize": 7,
    "rangeRetryWindowSizes": [4, 1],
    "initialCursor": "2000-01-01T00:00:00",
    "notes": "Central person entity in Acute. Source identifier is the client URI, for example /clients/123. Reads are executed in 7-day windows. On retry, the same window start is retried with 4-day and then 1-day windows."
  },
  {
    "key": "organisation",
    "label": "Organisation",
    "endpoint": "/organisations",
    "cron": "0 * * * *",
    "mode": "full",
    "enabled": false,
    "externalIdField": "orgId",
    "sourceUpdatedAtField": "latestSaveDate",
    "notes": "Company master entity for TTH organisation clients. The listed GET endpoint does not expose a modified-after filter in the current spec."
  },
  {
    "key": "employer",
    "label": "Employer",
    "endpoint": "/employers",
    "cron": "0 * * * *",
    "mode": "full",
    "enabled": false,
    "externalIdField": "employer",
    "notes": "Source bridge between Client and Organisation. Employer.clientId refers to the id part inside Client.client. The listed GET endpoint does not expose a modified-after filter in the current spec."
  },
  {
    "key": "invoice",
    "label": "Invoice",
    "endpoint": "/invoices",
    "cron": "*/30 * * * *",
    "mode": "incremental",
    "enabled": false,
    "externalIdField": "invoiceId",
    "sourceUpdatedAtField": "modifiedDate",
    "incrementalQueryParam": "modifiedAfter",
    "staticParams": {
      "dateType": "invoiceDate"
    },
    "notes": "MF-issued outbound sales invoice. Payer is identified by payerClientId or payerOrgId. The endpoint supports incremental filtering with modifiedAfter."
  },
  {
    "key": "invoiceEvent",
    "label": "InvoiceEvent",
    "endpoint": "/invoices",
    "cron": "*/30 * * * *",
    "mode": "incremental",
    "enabled": false,
    "staticParams": {
      "showEvents": true,
      "dateType": "invoiceDate"
    },
    "recordPath": "events",
    "recordContextParentFields": ["invoiceNo", "payerClientId", "payerOrgId", "personnelId", "unitId", "legalUnit"],
    "externalIdField": "eventPayerId",
    "sourceUpdatedAtField": "created",
    "incrementalQueryParam": "modifiedAfter",
    "notes": "Preferred analytical billed service line entity sourced from Invoice.events[]. The invoice endpoint must be called with showEvents=true."
  },
  {
    "key": "personnel",
    "label": "Personnel",
    "endpoint": "/personnel",
    "cron": "0 */4 * * *",
    "mode": "full",
    "enabled": false,
    "externalIdField": "personnelId",
    "notes": "MF staff and professionals, not patients and not employer employees. The listed GET endpoint does not expose a modified-after filter in the current spec."
  }
]
